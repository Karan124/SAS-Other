<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Payments Controls PoC — Our Approach</title>
<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --dark1: #181B25;
    --purple: #9819D7;
    --lightpurple: #DDBAFF;
    --light2: #E8E8ED;
    --red: #DA1710;
    --navy: #1F1B4F;
    --green: #22c55e;
    --card: rgba(255,255,255,0.04);
    --border: rgba(255,255,255,0.08);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'Outfit', sans-serif;
    background: var(--dark1);
    color: #fff;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 40px 32px;
  }

  .infographic {
    width: 1160px;
    max-width: 100%;
  }

  /* ── HEADER ── */
  .header {
    text-align: center;
    margin-bottom: 40px;
  }
  .header-kicker {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--lightpurple);
    margin-bottom: 10px;
  }
  .header-title {
    font-size: 38px;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 10px;
  }
  .header-title span { color: var(--purple); }
  .header-sub {
    font-size: 15px;
    color: rgba(255,255,255,0.45);
    font-weight: 400;
  }

  /* ── PHASE CONNECTOR ── */
  .phase-row {
    display: grid;
    grid-template-columns: 1fr 56px 1fr;
    gap: 0;
    align-items: stretch;
  }

  .connector {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding-top: 60px;
  }
  .connector-line {
    width: 1px;
    flex: 1;
    max-height: 40px;
    background: linear-gradient(to bottom, transparent, rgba(152,25,215,0.4));
  }
  .connector-arrow {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: rgba(152,25,215,0.2);
    border: 1px solid rgba(152,25,215,0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    color: var(--lightpurple);
  }
  .connector-line-2 {
    width: 1px;
    flex: 1;
    max-height: 40px;
    background: linear-gradient(to bottom, rgba(152,25,215,0.4), transparent);
  }

  /* ── PANELS ── */
  .panel {
    background: rgba(255,255,255,0.03);
    border: 0.5px solid var(--border);
    border-radius: 20px;
    padding: 32px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .panel-header {}
  .phase-badge {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 5px 12px;
    border-radius: 99px;
    margin-bottom: 12px;
  }
  .badge-done {
    background: rgba(34,197,94,0.15);
    border: 0.5px solid rgba(34,197,94,0.3);
    color: #86efac;
  }
  .badge-next {
    background: rgba(152,25,215,0.2);
    border: 0.5px solid rgba(152,25,215,0.4);
    color: var(--lightpurple);
  }
  .badge-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
  }
  .badge-done .badge-dot { background: #86efac; }
  .badge-next .badge-dot { background: var(--purple); animation: pulse 2s infinite; }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  .panel-title {
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 6px;
    line-height: 1.2;
  }
  .panel-title span { color: var(--purple); }
  .panel-desc {
    font-size: 13.5px;
    color: rgba(255,255,255,0.5);
    line-height: 1.6;
  }

  /* ── TF-IDF WORD CLOUD ── */
  .word-section-label {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.3);
    margin-bottom: 14px;
  }

  .word-arena {
    background: rgba(0,0,0,0.2);
    border: 0.5px solid rgba(255,255,255,0.06);
    border-radius: 14px;
    padding: 24px 20px;
    position: relative;
  }

  .word-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 10px 14px;
    align-items: center;
    justify-content: center;
    min-height: 140px;
  }

  .word {
    font-family: 'DM Mono', monospace;
    font-weight: 500;
    transition: all 0.3s ease;
    cursor: default;
    line-height: 1;
  }

  /* HIGH score — big, bright purple */
  .word.high {
    font-size: 22px;
    color: var(--lightpurple);
    text-shadow: 0 0 20px rgba(152,25,215,0.6);
  }
  /* MEDIUM score */
  .word.medium {
    font-size: 15px;
    color: rgba(255,255,255,0.65);
  }
  /* LOW score — small, faded */
  .word.low {
    font-size: 11px;
    color: rgba(255,255,255,0.22);
    text-decoration: line-through;
    text-decoration-color: rgba(218,23,16,0.5);
  }

  .word:hover { opacity: 0.8; }

  /* Score legend */
  .score-legend {
    display: flex;
    gap: 20px;
    margin-top: 16px;
    justify-content: center;
  }
  .legend-item {
    display: flex;
    align-items: center;
    gap: 7px;
    font-size: 11px;
    color: rgba(255,255,255,0.45);
  }
  .legend-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
  }

  /* ── TF-IDF HOW IT WORKS ── */
  .how-it-works {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }
  .how-card {
    background: rgba(0,0,0,0.2);
    border: 0.5px solid rgba(255,255,255,0.06);
    border-radius: 12px;
    padding: 14px 16px;
  }
  .how-card-title {
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 6px;
  }
  .how-card-body {
    font-size: 12.5px;
    color: rgba(255,255,255,0.55);
    line-height: 1.55;
  }
  .how-card-body strong {
    color: rgba(255,255,255,0.8);
    font-weight: 500;
  }

  /* ── TF-IDF RESULT ── */
  .result-bar {
    background: rgba(0,0,0,0.2);
    border: 0.5px solid rgba(255,255,255,0.06);
    border-radius: 12px;
    padding: 16px 18px;
    display: flex;
    align-items: center;
    gap: 16px;
  }
  .result-num {
    font-size: 32px;
    font-weight: 800;
    color: var(--purple);
    font-family: 'DM Mono', monospace;
    white-space: nowrap;
  }
  .result-label {
    font-size: 13px;
    color: rgba(255,255,255,0.6);
    line-height: 1.5;
  }
  .result-label strong {
    color: #fff;
    font-weight: 600;
    display: block;
    font-size: 14px;
    margin-bottom: 2px;
  }
  .result-divider {
    width: 0.5px;
    height: 40px;
    background: rgba(255,255,255,0.1);
    flex-shrink: 0;
  }

  /* ── LLM FLOW ── */
  .llm-flow {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  /* Input control card */
  .control-card {
    background: rgba(0,0,0,0.25);
    border: 0.5px solid rgba(255,255,255,0.08);
    border-radius: 12px;
    padding: 16px 18px;
  }
  .control-card-label {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.3);
    margin-bottom: 10px;
  }
  .control-field {
    margin-bottom: 8px;
  }
  .control-field:last-child { margin-bottom: 0; }
  .field-name {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--lightpurple);
    margin-bottom: 3px;
  }
  .field-value {
    font-size: 12.5px;
    color: rgba(255,255,255,0.7);
    line-height: 1.5;
  }

  /* Arrow */
  .flow-arrow {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 0 8px;
  }
  .flow-arrow-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, rgba(152,25,215,0.3), rgba(152,25,215,0.8));
  }
  .flow-arrow-label {
    font-size: 11px;
    color: var(--lightpurple);
    font-weight: 500;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .flow-arrow-tip {
    width: 0;
    height: 0;
    border-top: 5px solid transparent;
    border-bottom: 5px solid transparent;
    border-left: 8px solid rgba(152,25,215,0.8);
  }

  /* LLM brain */
  .llm-brain {
    background: linear-gradient(135deg, rgba(152,25,215,0.15), rgba(31,27,79,0.4));
    border: 0.5px solid rgba(152,25,215,0.4);
    border-radius: 14px;
    padding: 16px 18px;
    display: flex;
    align-items: center;
    gap: 14px;
    position: relative;
    overflow: hidden;
  }
  .llm-brain::before {
    content: '';
    position: absolute;
    top: -30px; right: -30px;
    width: 100px; height: 100px;
    border-radius: 50%;
    background: rgba(152,25,215,0.1);
  }
  .brain-icon {
    font-size: 28px;
    flex-shrink: 0;
  }
  .brain-text {}
  .brain-title {
    font-size: 14px;
    font-weight: 600;
    color: var(--lightpurple);
    margin-bottom: 3px;
  }
  .brain-desc {
    font-size: 12px;
    color: rgba(255,255,255,0.5);
    line-height: 1.5;
  }
  .brain-steps {
    display: flex;
    gap: 6px;
    margin-top: 8px;
    flex-wrap: wrap;
  }
  .brain-step {
    font-size: 10px;
    font-weight: 500;
    color: var(--lightpurple);
    background: rgba(152,25,215,0.2);
    border-radius: 99px;
    padding: 3px 9px;
  }

  /* Output card */
  .output-card {
    background: rgba(34,197,94,0.05);
    border: 0.5px solid rgba(34,197,94,0.25);
    border-radius: 12px;
    padding: 16px 18px;
  }
  .output-card-label {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #86efac;
    margin-bottom: 12px;
  }
  .output-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin-bottom: 12px;
  }
  .output-field {}
  .output-field-name {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(255,255,255,0.3);
    margin-bottom: 3px;
  }
  .output-field-value {
    font-size: 14px;
    font-weight: 700;
    color: #fff;
    line-height: 1.2;
  }
  .output-field-value.highlight {
    color: #86efac;
  }
  .confidence-bar-wrap {
    margin-top: 2px;
  }
  .confidence-bar-track {
    height: 4px;
    background: rgba(255,255,255,0.1);
    border-radius: 2px;
    overflow: hidden;
    margin-top: 5px;
  }
  .confidence-bar-fill {
    height: 100%;
    background: linear-gradient(to right, #86efac, var(--green));
    border-radius: 2px;
    width: 94%;
  }
  .rationale-box {
    background: rgba(0,0,0,0.2);
    border-radius: 8px;
    padding: 10px 12px;
  }
  .rationale-label {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(255,255,255,0.3);
    margin-bottom: 5px;
  }
  .rationale-text {
    font-size: 12px;
    color: rgba(255,255,255,0.65);
    line-height: 1.6;
    font-style: italic;
  }
  .rationale-text mark {
    background: rgba(152,25,215,0.3);
    color: var(--lightpurple);
    border-radius: 3px;
    padding: 0 3px;
    font-style: normal;
    font-weight: 500;
  }

  /* ── FOOTER ── */
  .footer {
    margin-top: 28px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 4px;
  }
  .footer-note {
    font-size: 11px;
    color: rgba(255,255,255,0.18);
    font-family: 'DM Mono', monospace;
  }
  .footer-badge {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-size: 11px;
    font-weight: 500;
    color: var(--lightpurple);
    background: rgba(152,25,215,0.1);
    border: 0.5px solid rgba(152,25,215,0.25);
    border-radius: 99px;
    padding: 5px 14px;
  }
  .footer-badge::before {
    content: '';
    width: 5px; height: 5px;
    border-radius: 50%;
    background: var(--purple);
  }
</style>
</head>
<body>

<div class="infographic">

  <!-- Header -->
  <div class="header">
    <div class="header-kicker">Payments Controls PoC — Sprint Demo</div>
    <div class="header-title">How we identify and classify <span>payment controls</span></div>
    <div class="header-sub">Two complementary techniques — statistical filtering followed by intelligent classification</div>
  </div>

  <!-- Two panels with connector -->
  <div class="phase-row">

    <!-- LEFT PANEL: TF-IDF -->
    <div class="panel">

      <div class="panel-header">
        <div class="phase-badge badge-done">
          <span class="badge-dot"></span>
          Phase 1 — Complete
        </div>
        <div class="panel-title">TF-IDF <span>Filtering</span></div>
        <div class="panel-desc">Words that rarely appear across all controls but frequently appear in payment controls are scored highest. Controls with low payment-word density are flagged and removed.</div>
      </div>

      <!-- Word cloud -->
      <div>
        <div class="word-section-label">Word importance across 7,000+ control descriptions</div>
        <div class="word-arena">
          <div class="word-cloud">
            <span class="word high">BPAY</span>
            <span class="word medium">transaction</span>
            <span class="word low">ensures</span>
            <span class="word high">sanctions</span>
            <span class="word low">management</span>
            <span class="word high">nostro</span>
            <span class="word medium">authorisation</span>
            <span class="word low">system</span>
            <span class="word high">SWIFT</span>
            <span class="word low">process</span>
            <span class="word medium">reconciliation</span>
            <span class="word high">settlement</span>
            <span class="word low">review</span>
            <span class="word medium">payment</span>
            <span class="word high">NPP</span>
            <span class="word low">control</span>
            <span class="word medium">clearing</span>
            <span class="word high">RTGS</span>
            <span class="word low">validates</span>
            <span class="word medium">remittance</span>
          </div>
          <div class="score-legend">
            <div class="legend-item">
              <div class="legend-dot" style="background:var(--lightpurple);"></div>
              High score — rare &amp; payment-specific
            </div>
            <div class="legend-item">
              <div class="legend-dot" style="background:rgba(255,255,255,0.5);"></div>
              Medium score
            </div>
            <div class="legend-item">
              <div class="legend-dot" style="background:rgba(255,255,255,0.2); border: 0.5px solid rgba(218,23,16,0.4);"></div>
              Low score — too common, discounted
            </div>
          </div>
        </div>
      </div>

      <!-- How it works -->
      <div class="how-it-works">
        <div class="how-card">
          <div class="how-card-title" style="color:var(--lightpurple);">Word appears often here</div>
          <div class="how-card-body">"Sanctions" appears in <strong>this control's description</strong> 3 times. Potentially important signal.</div>
        </div>
        <div class="how-card">
          <div class="how-card-title" style="color:#86efac;">Word is rare everywhere else</div>
          <div class="how-card-body">"Sanctions" appears in only <strong>8% of all 7,000 controls</strong>. High score — confirmed payment signal.</div>
        </div>
        <div class="how-card">
          <div class="how-card-title" style="color:rgba(255,255,255,0.4);">Word appears often here</div>
          <div class="how-card-body">"System" appears frequently in <strong>this control's description</strong>. Looks important.</div>
        </div>
        <div class="how-card">
          <div class="how-card-title" style="color:var(--red);">But common everywhere</div>
          <div class="how-card-body">"System" appears in <strong>91% of all controls</strong>. Low score — not a payment signal. Discounted.</div>
        </div>
      </div>

      <!-- Result -->
      <div class="result-bar">
        <div>
          <div style="font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; color:rgba(255,255,255,0.3); margin-bottom:4px;">Started with</div>
          <div class="result-num" style="color:rgba(255,255,255,0.4); font-size:24px;">2,000</div>
        </div>
        <div class="result-divider"></div>
        <div style="flex:1; text-align:center; font-size:20px; color:var(--purple);">→</div>
        <div class="result-divider"></div>
        <div>
          <div style="font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; color:rgba(255,255,255,0.3); margin-bottom:4px;">Payment controls retained</div>
          <div class="result-num">~1,300</div>
        </div>
        <div class="result-divider"></div>
        <div>
          <div style="font-size:11px; font-weight:600; text-transform:uppercase; letter-spacing:0.08em; color:rgba(255,255,255,0.3); margin-bottom:4px;">Low-payment controls flagged</div>
          <div class="result-num" style="color:var(--red); font-size:24px;">~700</div>
        </div>
      </div>

    </div>

    <!-- Connector -->
    <div class="connector">
      <div class="connector-line"></div>
      <div class="connector-arrow">→</div>
      <div class="connector-line-2"></div>
    </div>

    <!-- RIGHT PANEL: LLM -->
    <div class="panel">

      <div class="panel-header">
        <div class="phase-badge badge-next">
          <span class="badge-dot"></span>
          Phase 2 — In Progress
        </div>
        <div class="panel-title">LLM <span>Classification</span></div>
        <div class="panel-desc">Each of the 1,300 controls is sent to the LLM with the full text — name, objective, description, and test procedure. The model reads the context and maps the control to one of 28 Master Control outcomes.</div>
      </div>

      <!-- Flow -->
      <div class="llm-flow">

        <!-- Input -->
        <div class="control-card">
          <div class="control-card-label">① Control sent to LLM</div>
          <div class="control-field">
            <div class="field-name">Control Name</div>
            <div class="field-value">Daily reconciliation of outbound SWIFT messages</div>
          </div>
          <div class="control-field">
            <div class="field-name">Objective</div>
            <div class="field-value">Ensure all outbound SWIFT messages are reconciled against the core banking system before end of day to confirm completeness</div>
          </div>
          <div class="control-field">
            <div class="field-name">Control Type · Automation</div>
            <div class="field-value">Detective · Manual</div>
          </div>
        </div>

        <!-- Arrow -->
        <div class="flow-arrow">
          <div class="flow-arrow-line"></div>
          <div class="flow-arrow-label">② LLM reads full context</div>
          <div class="flow-arrow-line"></div>
          <div class="flow-arrow-tip"></div>
        </div>

        <!-- LLM brain -->
        <div class="llm-brain">
          <div class="brain-icon">🧠</div>
          <div class="brain-text">
            <div class="brain-title">GPT-5.2 — Understands regulatory intent</div>
            <div class="brain-desc">Reads all available text fields. Compares against all 28 Master Control definitions. Does not guess — cites evidence from the control text.</div>
            <div class="brain-steps">
              <span class="brain-step">Reads 3,000 words</span>
              <span class="brain-step">Compares to 28 CTs</span>
              <span class="brain-step">Temperature = 0</span>
              <span class="brain-step">Cites evidence</span>
            </div>
          </div>
        </div>

        <!-- Arrow -->
        <div class="flow-arrow">
          <div class="flow-arrow-line"></div>
          <div class="flow-arrow-label">③ Returns structured result</div>
          <div class="flow-arrow-line"></div>
          <div class="flow-arrow-tip"></div>
        </div>

        <!-- Output -->
        <div class="output-card">
          <div class="output-card-label">✓ Classification output</div>
          <div class="output-grid">
            <div class="output-field">
              <div class="output-field-name">Master Control Outcome</div>
              <div class="output-field-value highlight">CT08 — Transaction Reconciliation</div>
            </div>
            <div class="output-field">
              <div class="output-field-name">Confidence</div>
              <div class="output-field-value">94%</div>
              <div class="confidence-bar-wrap">
                <div class="confidence-bar-track">
                  <div class="confidence-bar-fill"></div>
                </div>
              </div>
            </div>
          </div>
          <div class="rationale-box">
            <div class="rationale-label">Rationale — grounded in control text</div>
            <div class="rationale-text">"The control explicitly states <mark>reconciled against the core banking system</mark> to confirm <mark>completeness</mark> of outbound messages — directly satisfying CT08 transaction reconciliation requirements."</div>
          </div>
        </div>

      </div>

    </div>

  </div>

  <!-- Footer -->
  <div class="footer">
    <div class="footer-note">Payments Controls PoC · Sprint Demo · June 2026</div>
    <div class="footer-badge">1,300 controls · 28 Master Control outcomes · GPT-5.2 via Azure APIM</div>
  </div>

</div>

</body>
</html>